#include <iostream> //connect library "iostream" to make text appearance on screen possible

int main() 
{
    std::cout << "Hello World!" << std::endl; //"std" is a name of a space of names. It also could be placed in head as "using namespace std;" instead of writing it all the time
    return 0; //program must return any integer number... is it realy needed? In default example there is no such thing
}
